<html>
    <head>
        <link rel="stylesheet" href="https://pyscript.net/alpha/pyscript.css" />
        <script defer src="https://pyscript.net/alpha/pyscript.js"></script>
        <link href="https://cdn.jsdeliver.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
    </head>

    <body>

        <div>Type the Username here</div>
  <input type="text" id="test-input" style="border: 2px solid #000"/>
  <button id="submit-button" type="submit" pys-onClick="my_function">OK</button>
  <div id="test-output"></div>

        <py-env>
            - pandas
            - paths:
                - ./username.csv
        </py-env>

        <py-script>
import pandas as pd
import csv

def my_function(*args, **kwargs):

    lst=[*csv.DictReader(open("./username.csv"))]; print(lst)
    
    text = Element('test-input').element.value
    

    for row in lst:
            if row['Username']==text:
                text="for user= "+text+" = "+str(row)

    Element('test-output').element.innerText = text
        </py-script>
    </body>
